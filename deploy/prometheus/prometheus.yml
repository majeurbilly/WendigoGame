global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  # Décommenter lorsque le backend exposera GET /metrics (Ticket applicatif).
  # - job_name: wendigo-backend
  #   static_configs:
  #     - targets: ["backend:8080"]
  #   metrics_path: /metrics

{
  "name": "wendigo-authentik",
  "version": "1.0.0",
  "private": true,
  "description": "Pulumi IaC for Wendigo — Authentik Helm on K3s + OIDC lookup",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "biome check .",
    "fix": "biome check --write .",
    "preview": "pulumi preview",
    "up": "pulumi up",
    "destroy": "pulumi destroy"
  },
  "dependencies": {
    "@pulumi/authentik": "file:sdks/authentik",
    "@pulumi/kubernetes": "^4.23.0",
    "@pulumi/pulumi": "^3.192.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.15",
    "@types/node": "^20",
    "typescript": "^5.6.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@pulumi/kubernetes",
      "@pulumi/authentik"
    ]
  }
}
